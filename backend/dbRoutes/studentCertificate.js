const database = require('../database')
const {findCourseById} = require('./courses')

//for viewing the certificate in students side. Only with student key
const dummyCertificateRelease=async()=>{
    
}

module.exports = {
    dummyCertificateRelease,
}